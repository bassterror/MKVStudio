<Window x:Class="MKVStudio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MKVStudio"
        xmlns:controls="clr-namespace:MKVStudio.Views.Controls"
        xmlns:viewmodels="clr-namespace:MKVStudio.ViewModels"        
        d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <controls:MainNavigator Grid.Column="0" DataContext="{Binding Navigator}"/>
        <ContentControl Grid.Column="1" Content="{Binding Navigator.CurrentViewModel}"/>
    </Grid>
</Window>
