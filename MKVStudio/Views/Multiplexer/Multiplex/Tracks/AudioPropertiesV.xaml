<UserControl x:Class="MKVStudio.Views.AudioPropertiesV"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:MKVStudio.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:AudioPropertiesVM}"
             mc:Ignorable="d"
             d:DesignHeight="350"
             d:DesignWidth="550">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0"
                   HorizontalContentAlignment="Right"
                   Margin="2">
                <TextBlock Text="Track name:"
                           Foreground="{StaticResource ControlDisabledGlythColour}" />
            </Label>
            <TextBox Grid.Column="1"
                     Text="{Binding AudioProperties.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     VerticalContentAlignment="Center"
                     Margin="2" />
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0"
                   HorizontalContentAlignment="Right"
                   Margin="2">
                <TextBlock Text="ID:"
                           Foreground="{StaticResource ControlDisabledGlythColour}" />
            </Label>
            <TextBlock Grid.Column="1"
                       Text="{Binding AudioProperties.ID}"
                       VerticalAlignment="Center"
                       Margin="2" />
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0"
                   HorizontalContentAlignment="Right"
                   Margin="2">
                <TextBlock Text="Codec:"
                           Foreground="{StaticResource ControlDisabledGlythColour}" />
            </Label>
            <TextBlock Grid.Column="1"
                       Text="{Binding AudioProperties.Codec}"
                       VerticalAlignment="Center"
                       Margin="2" />
        </Grid>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0"
                   HorizontalContentAlignment="Right"
                   Margin="2">
                <TextBlock Text="Codec ID:"
                           Foreground="{StaticResource ControlDisabledGlythColour}" />
            </Label>
            <TextBlock Grid.Column="1"
                       Text="{Binding AudioProperties.CodecId}"
                       VerticalAlignment="Center"
                       Margin="2" />
        </Grid>
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0"
                   HorizontalContentAlignment="Right"
                   Margin="2">
                <TextBlock Text="Channels:"
                           Foreground="{StaticResource ControlDisabledGlythColour}" />
            </Label>
            <TextBlock Grid.Column="1"
                       Text="{Binding AudioProperties.Channels}"
                       VerticalAlignment="Center"
                       Margin="2" />
        </Grid>
        <Grid Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0"
                   HorizontalContentAlignment="Right"
                   Margin="2">
                <TextBlock Text="Sample rate:"
                           Foreground="{StaticResource ControlDisabledGlythColour}" />
            </Label>
            <TextBlock Grid.Column="1"
                       Text="{Binding AudioProperties.SampleRate}"
                       VerticalAlignment="Center"
                       Margin="2" />
        </Grid>
        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0"
                   HorizontalContentAlignment="Right"
                   Margin="2">
                <TextBlock Text="Language:"
                           Foreground="{StaticResource ControlDisabledGlythColour}" />
            </Label>
            <ComboBox Grid.Column="1"
                      Margin="2"
                      ItemsSource="{Binding Util.Preferences.Languages.PreferedLanguages}"
                      SelectedItem="{Binding AudioProperties.Language, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding ID}" />
                            <TextBlock Text=", " />
                            <TextBlock Text="{Binding Name}" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>
        <Grid Grid.Row="7">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <CheckBox Grid.Column="0"
                      Grid.Row="0"
                      IsChecked="{Binding AudioProperties.EnabledTrack, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      VerticalContentAlignment="Center"
                      Margin="2">
                <TextBlock>
                    <TextBlock.Resources>
                        <Style TargetType="{x:Type TextBlock}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type CheckBox}}, Path=IsChecked}"
                                             Value="False">
                                    <Setter Property="Foreground"
                                            Value="{StaticResource ControlDisabledGlythColour}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Resources>
                    Enabled track
                </TextBlock>
            </CheckBox>
            <CheckBox Grid.Column="0"
                      Grid.Row="1"
                      IsChecked="{Binding AudioProperties.DefaultTrack, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      VerticalContentAlignment="Center"
                      Margin="2">
                <TextBlock>
                    <TextBlock.Resources>
                        <Style TargetType="{x:Type TextBlock}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type CheckBox}}, Path=IsChecked}"
                                             Value="False">
                                    <Setter Property="Foreground"
                                            Value="{StaticResource ControlDisabledGlythColour}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Resources>
                    Default track
                </TextBlock>
            </CheckBox>
            <CheckBox Grid.Column="0"
                      Grid.Row="2"
                      IsChecked="{Binding AudioProperties.FlagOriginal, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      VerticalContentAlignment="Center"
                      Margin="2">
                <TextBlock>
                    <TextBlock.Resources>
                        <Style TargetType="{x:Type TextBlock}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type CheckBox}}, Path=IsChecked}"
                                             Value="False">
                                    <Setter Property="Foreground"
                                            Value="{StaticResource ControlDisabledGlythColour}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Resources>
                    Original language
                </TextBlock>
            </CheckBox>
            <CheckBox Grid.Column="1"
                      Grid.Row="0"
                      IsChecked="{Binding AudioProperties.ForcedTrack, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      VerticalContentAlignment="Center"
                      Margin="2">
                <TextBlock>
                    <TextBlock.Resources>
                        <Style TargetType="{x:Type TextBlock}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type CheckBox}}, Path=IsChecked}"
                                             Value="False">
                                    <Setter Property="Foreground"
                                            Value="{StaticResource ControlDisabledGlythColour}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Resources>
                    Forced track
                </TextBlock>
            </CheckBox>
            <CheckBox Grid.Column="1"
                      Grid.Row="1"
                      IsChecked="{Binding AudioProperties.FlagCommentary, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      VerticalContentAlignment="Center"
                      Margin="2">
                <TextBlock>
                    <TextBlock.Resources>
                        <Style TargetType="{x:Type TextBlock}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type CheckBox}}, Path=IsChecked}"
                                             Value="False">
                                    <Setter Property="Foreground"
                                            Value="{StaticResource ControlDisabledGlythColour}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Resources>
                    Commentary track
                </TextBlock>
            </CheckBox>
            <CheckBox Grid.Column="1"
                      Grid.Row="2"
                      IsChecked="{Binding AudioProperties.FlagVisualImpaired, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      VerticalContentAlignment="Center"
                      Margin="2">
                <TextBlock>
                    <TextBlock.Resources>
                        <Style TargetType="{x:Type TextBlock}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type CheckBox}}, Path=IsChecked}"
                                             Value="False">
                                    <Setter Property="Foreground"
                                            Value="{StaticResource ControlDisabledGlythColour}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Resources>
                    Visual impaired track
                </TextBlock>
            </CheckBox>
        </Grid>
    </Grid>
</UserControl>
