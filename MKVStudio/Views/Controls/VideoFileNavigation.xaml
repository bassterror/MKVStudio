<UserControl x:Class="MKVStudio.Views.Controls.VideoFileNavigation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:s="clr-namespace:MKVStudio.State"
             xmlns:vm="clr-namespace:MKVStudio.ViewModels"
             xmlns:c="clr-namespace:MKVStudio.Converters"
             d:DataContext="{d:DesignInstance Type=S:Navigator}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <c:EqualValueToParameterConverter x:Key="EVTPC"/>
    </UserControl.Resources>
    <Grid>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <RadioButton Content="General" Margin="5" 
                         IsChecked="{Binding CurrentVideoFileViewModel, Mode=OneWay, Converter={StaticResource EVTPC}, ConverterParameter={x:Type vm:GeneralViewModel}}"
                         Command="{Binding UpdateCurrentVideoFileViewModelCommand}" CommandParameter="{x:Static s:ViewModelTypes.General}"/>
            <RadioButton Content="Media info" Margin="5"
                         IsChecked="{Binding CurrentVideoFileViewModel, Mode=OneWay, Converter={StaticResource EVTPC}, ConverterParameter={x:Type vm:MediaInfoViewModel}}"
                         Command="{Binding UpdateCurrentVideoFileViewModelCommand}" CommandParameter="{x:Static s:ViewModelTypes.MediaInfo}"/>
            <RadioButton Content="Convert" Margin="5"
                         IsChecked="{Binding CurrentVideoFileViewModel, Mode=OneWay, Converter={StaticResource EVTPC}, ConverterParameter={x:Type vm:ConvertViewModel}}"
                         Command="{Binding UpdateCurrentVideoFileViewModelCommand}" CommandParameter="{x:Static s:ViewModelTypes.Convert}"/>
        </StackPanel>
    </Grid>
</UserControl>
