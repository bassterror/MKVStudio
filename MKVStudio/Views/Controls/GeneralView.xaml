<UserControl x:Class="MKVStudio.Views.Controls.GeneralView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:MKVStudio.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Grid.Row="1">
            <TextBlock Text="{Binding Navigator.SelectedVideo.InputFullName}"/>
            <TextBlock Text="{Binding Navigator.SelectedVideo.InputI}"/>
            <TextBlock Text="{Binding Navigator.SelectedVideo.InputLRA}"/>
            <TextBlock Text="{Binding Navigator.SelectedVideo.Channels}"/>
            <TextBlock Text="{Binding Navigator.SelectedVideo.SampleRates}"/>
            <TextBlock Text="{Binding Navigator.SelectedVideo.OutputPath}"/>
            <Button Content="FirstPass" Command="{Binding Navigator.SelectedVideo.RunFirstPassCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
