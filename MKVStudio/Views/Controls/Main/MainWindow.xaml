<Window x:Class="MKVStudio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:c="clr-namespace:MKVStudio.Views.Controls"
        xmlns:vm="clr-namespace:MKVStudio.ViewModels"        
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        mc:Ignorable="d"
        Title="MKV Studio"
        Height="800"
        Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <c:MainNavigation Grid.Column="0" DataContext="{Binding ThisMainViewModel}" Margin="1"/>
        <ContentControl Grid.Column="1" Content="{Binding CurrentMainViewModel}" Margin="1"/>
    </Grid>
</Window>
